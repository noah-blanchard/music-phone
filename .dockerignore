# Both images build from this one context — the server from
# apps/server/Dockerfile, the web app from apps/web/Dockerfile — so every
# workspace's source has to reach it. Only what a build reproduces is excluded.
node_modules
**/node_modules
.next
**/.next
dist
**/dist
.git
.github
*.tsbuildinfo
.env
.env.*
*.md
