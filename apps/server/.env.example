# Port the Elysia server listens on (Render injects PORT automatically).
PORT=3001

# Allowed CORS origin for the web client. Use the Vercel URL in production,
# e.g. https://musicphone.vercel.app . Defaults to "*" for local development.
WEB_ORIGIN=http://localhost:3000
